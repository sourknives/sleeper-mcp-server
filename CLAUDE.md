# sleeper-mcp-server

## Agent skills

### Issue tracker

Issues live as GitHub issues in `sourknives/sleeper-mcp-server`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary, label strings unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
